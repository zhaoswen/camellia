import 'package:flutter/material.dart';

// 画布标准
class CanvasDef {
  // 画布背景颜色
  static Color canvasBackgroundColor = Colors.white;

  // 节点背景颜色
  static Color nodeBackgroundColor = Colors.white;

  // 节点字体颜色
  static Color nodeTextColor = Colors.black87;

  // 节点边框颜色（默认与背景色一致）
  static Color nodeBorderColor = Colors.black12;

  // 节点默认高度
  static double nodeDefaultWidth = 130;

  // 节点默认宽度
  static double nodeDefaultHeight = 50;

  // 节点圆角
  static double nodeDefaultRadius = 8;

  // 节点默认标题字体大小
  static double nodeDefaultTitleFontSize = 14;
}
